defmodule NonExistentSourceTest do
  use ExUnit.Case, async: true

  # The corresponding source file for this test (e.g., lib/non_existent_source.ex)
  # is intentionally missing in the fixtures.

  test "example" do
    assert true
  end
end
