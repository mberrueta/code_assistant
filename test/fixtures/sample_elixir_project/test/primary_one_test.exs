defmodule PrimaryOneTest do
  use ExUnit.Case, async: true

  # This is a dummy test file for lib/primary_one.ex
  # It might have its own aliases, but ElixirTests focuses on its source's aliases.

  # alias PrimaryOne.HelperA # This alias is unused in this dummy test.
  alias SomeExternalTestHelper

  test "example" do
    assert true
  end
end
